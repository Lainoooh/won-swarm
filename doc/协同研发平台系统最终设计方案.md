# 协同研发平台系统 - 最终设计方案

**版本：** V1.1
**创建日期：** 2026-03-21
**更新日期：** 2026-03-22
**状态：** 已完成 - 核心功能开发完成

---

## 一、系统架构

### 1.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                    协同研发平台 (公网)                           │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  FastAPI (HTTP + WebSocket) + SQLite (端口：30009)        │  │
│  │                                                            │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐   │  │
│  │  │ Agent 管理  │  │ 任务管理    │  │ WebSocket 管理器 │   │  │
│  │  │ (AK/SK)    │  │ (项目/需求) │  │ (连接 + 消息路由) │   │  │
│  │  └─────────────┘  └─────────────┘  └─────────────────┘   │  │
│  │                                                            │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐   │  │
│  │  │ 需求管理    │  │ 文档管理    │  │ 通知中心        │   │  │
│  │  │ (3 级层级)   │  │ (上传下载)  │  │ (消息推送)      │   │  │
│  │  └─────────────┘  └─────────────┘  └─────────────────┘   │  │
│  │                                                            │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐   │  │
│  │  │ 审计日志    │  │ 项目-Agent  │  │ 任务分配记录    │   │  │
│  │  │ (操作追踪)  │  │ (成员管理)  │  │ (历史记录)      │   │  │
│  │  └─────────────┘  └─────────────┘  └─────────────────┘   │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
          ▲                    ▲                    ▲
          │ WebSocket 连接      │ HTTP API (备用)     │ WebSocket 连接
          │ (主通道)           │ (降级方案)          │ (主通道)
          │                    │                    │
    ┌─────┴─────┐        ┌─────┴─────┐      ┌─────┴─────┐
    │ OpenCLAW  │        │ OpenCLAW  │      │ OpenCLAW  │
    │ Node-001  │        │ Node-002  │      │ Node-003  │
    │ (局域网)  │        │ (局域网)  │      │ (局域网)  │
    └───────────┘        └───────────┘        └───────────┘
```

### 1.2 组件依赖

```
协同研发平台：
├── Python 3.10+
├── fastapi
├── uvicorn
├── sqlalchemy (SQLite)
├── pyjwt
├── bcrypt
├── python-multipart
├── aiofiles
└── 无外部中间件！

OpenCLAW Agent:
├── OpenCLAW 运行时
├── websockets (或 httpx 带 WebSocket 支持)
└── 业务相关依赖

前端工程：
├── Vue 3
├── Vite
├── Element Plus (UI 组件库)
├── Pinia (状态管理)
├── Vue Router (路由管理)
└── Axios (HTTP 客户端)
```

---

## 二、角色管理设计

### 2.1 角色配置存储策略

```
┌─────────────────────────────────────────────────────────────────┐
│ OpenCLAW 本地配置                                                │
│ ~/.openclaw/role-config.json                                    │
├─────────────────────────────────────────────────────────────────┤
│ {                                                               │
│   "agentId": "pc2-worker",                                     │
│   "name": "PC2-项目经理",                                      │
│   "roles": ["project-manager"],                                │
│   "capabilities": ["task-decomposition", "progress-tracking"], │
│   "maxConcurrentTasks": 3                                      │
│ }                                                               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ 注册时上报
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 协同平台存储                                                     │
│ SQLite agents 表                                                 │
├─────────────────────────────────────────────────────────────────┤
│ id | name | roles(JSON) | status | current_task | sk_hash | ...│
│ uuid | PC2-项目经理 | ["project-manager"] | online | ... | ... │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ 运行时约束
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ OpenCLAW SKILL 行为约束                                          │
├─────────────────────────────────────────────────────────────────┤
│ 根据本地 roles 数组决定：                                         │
│ - 可领取的任务类型（角色匹配检查）                                │
│ - 可执行的操作（权限矩阵）                                        │
│ - 遵循的工作制度（SKILL 规范）                                    │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 角色定义

| 角色 | 英文 | 职责 |
|------|------|------|
| 项目经理 | `project-manager` | 任务拆解、分配、进度跟踪 |
| 产品经理 | `product-manager` | 需求分析、文档编写 |
| UI 设计师 | `ui-designer` | 界面设计、交互设计 |
| 前端研发 | `frontend-developer` | 前端开发、自测 |
| 后端研发 | `backend-developer` | 后端开发、自测 |
| 测试工程师 | `qa-engineer` | 测试用例、执行测试 |

### 2.3 角色权限矩阵

| 操作 | 项目经理 | 产品经理 | UI 设计 | 前端 | 后端 | 测试 |
|------|---------|---------|--------|------|------|------|
| 领取任务 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 拆解任务 | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ |
| 分配任务 | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| 需求分析 | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ |
| UI 设计 | ❌ | ❌ | ✅ | ❌ | ❌ | ❌ |
| 前端开发 | ❌ | ❌ | ❌ | ✅ | ❌ | ❌ |
| 后端开发 | ❌ | ❌ | ❌ | ❌ | ✅ | ❌ |
| 测试执行 | ✅ | ❌ | ❌ | ✅ | ✅ | ✅ |

**原则：** OpenCLAW 本地根据角色配置约束行为，禁止跨角色操作

---

## 三、认证与注册流程

### 3.1 完整流程

```
Step 1: 用户在平台创建 Agent
        ↓
        → 平台生成 API Key (AK)
        → 用户获取 AK 并配置到 OpenCLAW
        → 平台保存 Agent 记录（状态：未激活）

Step 2: OpenCLAW SKILL 启动
        ↓
        → 读取 ~/.openclaw/role-config.json
        → 检查 ~/.openclaw/platform-config.json (URL + AK)
        → 调用 POST /api/agents/register 注册

Step 3: 平台验证并颁发 Worker SK
        ↓
        → 验证 AK 有效性
        → 生成 Worker SK（运行态凭证）
        → 更新 Agent 状态为在线
        → 返回 worker_sk 和 agent_id

Step 4: OpenCLAW 保存 Worker SK
        ↓
        → 保存到 ~/.openclaw/worker-sk.json
        → 建立 WebSocket 连接

Step 5: 建立 WebSocket 连接
        ↓
        → WS /ws/agent/connect?sk=worker_sk_xxx
        → 平台验证 SK，建立长连接
        → 返回 agent_id 和 session_id

Step 6: 开始工作
        ↓
        → 平台通过 WebSocket 推送任务
        → Agent 通过 WebSocket 上报状态和心跳
```

### 3.2 凭证管理

```
┌─────────────────────────────────────────────────────────────────┐
│ 凭证类型与用途                                                   │
├──────────────────┬──────────────────┬───────────────────────────┤
│ 凭证类型         │ 用途             │ 存储位置                  │
├──────────────────┼──────────────────┼───────────────────────────┤
│ Platform AK      │ 注册时身份验证   │ ~/.openclaw/platform-config.json │
│ (用户配置)       │ (一次性的)       │ (用户通过 Web 界面获取后配置) │
├──────────────────┼──────────────────┼───────────────────────────┤
│ Worker SK        │ 运行时认证       │ ~/.openclaw/worker-sk.json │
│ (平台颁发)       │ WebSocket 连接   │ (注册后自动保存)           │
├──────────────────┼──────────────────┼───────────────────────────┤
│ role-config.json │ 角色定义和行为   │ ~/.openclaw/role-config.json │
│ (本地配置)       │ 约束             │ (用户预先配置)             │
└──────────────────┴──────────────────┴───────────────────────────┘
```

---

## 四、API 接口设计

### 4.1 Agent 管理接口

```
POST /api/agents/register
├── 功能：Agent 注册（AK 换 SK）
├── 请求头：Authorization: Bearer {platform_ak}
├── 请求体：
│   {
│     "agent_id": "pc2-worker",
│     "name": "PC2-项目经理",
│     "roles": ["project-manager"],
│     "capabilities": ["task-decomposition", "progress-tracking"],
│     "max_concurrent_tasks": 3
│   }
└── 响应：
    {
      "code": 200,
      "data": {
        "agent_id": "uuid-xxx",
        "worker_sk": "worker_sk_xxx",
        "register_url": "http://server:8000/ws/agent/connect"
      }
    }

POST /api/agents/login
├── 功能：Agent 登录（获取 worker_sk）
├── 请求头：Authorization: Bearer {platform_ak}
└── 响应：同 register

GET /api/agents
├── 功能：获取 Agent 列表
├── 查询参数：status, role, page, page_size
└── 响应：Agent 列表（含当前任务信息）

GET /api/agents/:id
├── 功能：获取 Agent 详情
└── 响应：Agent 详细信息（含 current_task）
```

### 4.2 WebSocket 连接

```
WS /ws/agent/connect
├── 查询参数：sk={worker_sk}
├── 连接建立响应：
│   {
│     "type": "connected",
│     "agent_id": "uuid-xxx",
│     "session_id": "session-xxx",
│     "server_time": "2026-03-21T10:00:00Z"
│   }
└── 后续消息通过 WebSocket 传输
```

### 4.3 任务管理接口

```
POST /api/tasks/:id/claim
├── 功能：领取任务
├── 请求头：Authorization: Bearer {worker_sk}
├── 请求体：{}
└── 响应：{ "code": 200, "data": { "task_id": "xxx", "status": "claimed" } }

POST /api/tasks/:id/status
├── 功能：上报任务状态
├── 请求头：Authorization: Bearer {worker_sk}
├── 请求体：
│   {
│     "status": "in_progress",
│     "progress": 50,
│     "message": "开发中..."
│   }
└── 响应：{ "code": 200 }

POST /api/tasks/:id/complete
├── 功能：完成任务
├── 请求头：Authorization: Bearer {worker_sk}
├── 请求体：
│   {
│     "status": "completed",
│     "result": {
│       "code_repository": "https://github.com/...",
│       "artifacts": [...],
│       "test_report": {...}
│     },
│     "message": "任务已完成"
│   }
└── 响应：{ "code": 200 }

GET /api/tasks/pending
├── 功能：获取待处理任务（HTTP 轮询备用方案）
├── 请求头：Authorization: Bearer {worker_sk}
└── 响应：{ "code": 200, "data": { "tasks": [...] } }

GET /api/tasks/kanban/view
├── 功能：获取看板视图（按状态分组）
└── 响应：{ "code": 200, "data": { "columns": {...} } }
```

### 4.4 需求管理接口

```
GET /api/requirements
├── 功能：获取需求列表（支持树形结构）
├── 查询参数：project_id, level, parent_id, tree, page, page_size
└── 响应：
    {
      "code": 200,
      "data": {
        "total": 10,
        "items": [...],
        "tree": true  // 树形结构
      }
    }

POST /api/requirements
├── 功能：创建需求（支持 Epic/Feature/Task）
├── 请求体：
│   {
│     "project_id": "uuid",
│     "title": "需求标题",
│     "level": "epic",  // epic/feature/task
│     "parent_id": "uuid",  // 父需求 ID
│     "epic_id": "uuid",    // 所属大需求 ID
│     "type": "new_feature",
│     "priority": "P1",
│     "document_ids": [...]
│   }
└── 响应：{ "code": 200, "data": { "id": "xxx" } }

GET /api/requirements/:id
├── 功能：获取需求详情
└── 响应：需求详细信息

PUT /api/requirements/:id
├── 功能：更新需求
└── 响应：{ "code": 200, "message": "需求已更新" }

DELETE /api/requirements/:id
├── 功能：删除需求（级联删除子需求）
└── 响应：{ "code": 200, "message": "需求已删除" }

GET /api/requirements/:id/children
├── 功能：获取子需求列表
└── 响应：{ "code": 200, "data": { "total": 3, "items": [...] } }

GET /api/requirements/:id/tasks
├── 功能：获取需求关联的执行任务
└── 响应：{ "code": 200, "data": { "total": 2, "items": [...] } }
```

### 4.5 文档管理接口

```
GET /api/documents
├── 功能：获取文档列表
├── 查询参数：project_id, requirement_id, task_id
└── 响应：文档列表

POST /api/documents/upload
├── 功能：上传文档
├── 请求体：multipart/form-data
└── 响应：{ "code": 200, "data": { "id": "xxx", "url": "..." } }

GET /api/documents/:id
├── 功能：获取文档详情
└── 响应：文档详细信息

GET /api/documents/:id/download
├── 功能：下载文档
└── 响应：文件流

DELETE /api/documents/:id
├── 功能：删除文档
└── 响应：{ "code": 200, "message": "文档已删除" }
```

### 4.6 审计日志接口

```
GET /api/audit-logs
├── 功能：获取审计日志列表
├── 查询参数：user_id, resource_type, action, page, page_size
└── 响应：审计日志列表
```

### 4.7 项目-Agent 关联接口

```
GET /api/projects/:id/agents
├── 功能：获取项目成员列表
└── 响应：{ "code": 200, "data": { "total": 3, "items": [...] } }

POST /api/projects/:id/agents
├── 功能：添加项目成员
├── 请求体：{ "agent_id": "uuid", "role": "frontend-developer" }
└── 响应：{ "code": 200, "message": "成员已添加" }

DELETE /api/projects/:id/agents/:agent_id
├── 功能：移除项目成员
└── 响应：{ "code": 200, "message": "成员已移除" }
```

### 4.8 通知中心接口

```
GET /api/notifications
├── 功能：获取通知列表
├── 查询参数：is_read, page, page_size
└── 响应：通知列表

GET /api/notifications/unread-count
├── 功能：获取未读通知数量
└── 响应：{ "code": 200, "data": { "count": 3 } }

POST /api/notifications/:id/read
├── 功能：标记通知为已读
└── 响应：{ "code": 200, "message": "已标记为已读" }

POST /api/notifications/read-all
├── 功能：标记所有通知为已读
└── 响应：{ "code": 200, "message": "所有通知已标记为已读" }
```

---

## 五、WebSocket 消息格式

### 5.1 平台 → Agent

**任务下发：**
```json
{
  "type": "task_assigned",
  "message_id": "msg-uuid",
  "timestamp": "2026-03-21T10:00:00Z",
  "data": {
    "task_id": "task-uuid",
    "requirement_id": "req-uuid",
    "project_id": "proj-uuid",
    "project_name": "电商平台重构",
    "title": "登录页面开发",
    "type": "frontend",
    "required_roles": ["frontend-developer"],
    "priority": "P1",
    "description": "...",
    "deadline": "2026-03-22T18:00:00Z",
    "attachments": [
      {"type": "requirement_doc", "url": "..."},
      {"type": "ui_design", "url": "..."}
    ]
  }
}
```

**心跳响应：**
```json
{
  "type": "heartbeat_ack",
  "server_time": "2026-03-21T10:00:00Z"
}
```

**任务状态变更通知：**
```json
{
  "type": "task_updated",
  "task_id": "task-uuid",
  "status": "cancelled",
  "reason": "需求变更"
}
```

### 5.2 Agent → 平台

**任务确认：**
```json
{
  "type": "task_ack",
  "message_id": "msg-uuid",
  "task_id": "task-uuid",
  "status": "accepted",
  "agent_id": "agent-uuid",
  "timestamp": "2026-03-21T10:00:01Z"
}
```

**心跳上报：**
```json
{
  "type": "heartbeat",
  "agent_id": "agent-uuid",
  "status": "idle",
  "current_task_id": "task-uuid",
  "sub_agents": [
    {"id": "sub-1", "purpose": "coding", "status": "running"}
  ],
  "timestamp": "2026-03-21T10:00:00Z"
}
```

**任务进度上报：**
```json
{
  "type": "task_progress",
  "task_id": "task-uuid",
  "progress": 50,
  "status": "in_progress",
  "message": "已完成页面布局开发",
  "timestamp": "2026-03-21T14:00:00Z"
}
```

**任务完成上报：**
```json
{
  "type": "task_completed",
  "task_id": "task-uuid",
  "status": "completed",
  "result": {
    "code_repository": "https://github.com/...",
    "artifacts": [...],
    "test_report": {...}
  },
  "message": "任务已完成，代码已提交",
  "timestamp": "2026-03-21T18:00:00Z"
}
```

---

## 六、数据模型设计

### 6.1 核心表结构

```sql
-- 管理员表
CREATE TABLE users (
    id VARCHAR(36) PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    is_active BOOLEAN DEFAULT TRUE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 管理员默认账号：admin / 123456

-- Agent 表
CREATE TABLE agents (
    id VARCHAR(36) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    platform_ak_hash VARCHAR(64) NOT NULL,  -- 平台 AK 哈希（注册用）
    worker_sk_hash VARCHAR(64),              -- Worker SK 哈希（运行用）
    roles JSON NOT NULL,                     -- 角色列表
    capabilities JSON,
    max_concurrent_tasks INTEGER DEFAULT 3,
    status VARCHAR(20) DEFAULT 'offline',    -- offline/idle/busy
    current_task_id VARCHAR(36),
    websocket_session_id VARCHAR(64),
    last_heartbeat DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 项目表
CREATE TABLE projects (
    id VARCHAR(36) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    manager_id VARCHAR(36),
    status VARCHAR(20) DEFAULT 'planning',   -- planning/in_progress/completed/archived
    start_date DATE,
    end_date DATE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 项目-Agent 关联表
CREATE TABLE project_agents (
    id VARCHAR(36) PRIMARY KEY,
    project_id VARCHAR(36) NOT NULL,
    agent_id VARCHAR(36) NOT NULL,
    role VARCHAR(50) NOT NULL,
    joined_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (project_id) REFERENCES projects(id),
    FOREIGN KEY (agent_id) REFERENCES agents(id)
);

-- 需求表 (3 级层级结构)
CREATE TABLE requirements (
    id VARCHAR(36) PRIMARY KEY,
    project_id VARCHAR(36) NOT NULL,
    title VARCHAR(200) NOT NULL,
    description TEXT,

    -- 3 级层级结构
    level VARCHAR(30) DEFAULT 'epic',      -- epic/feature/task
    parent_id VARCHAR(36),                 -- 父需求 ID (自引用外键)
    epic_id VARCHAR(36),                   -- 所属大需求 ID

    -- 分类和状态
    type VARCHAR(30) DEFAULT 'new_feature',  -- new_feature/change_request/fix_request/bug_fix/optimization
    priority VARCHAR(10) DEFAULT 'P2',       -- P0/P1/P2/P3
    status VARCHAR(20) DEFAULT 'pending',    -- pending/reviewing/approved/in_development/testing/completed

    -- 人员关联
    creator_id VARCHAR(36),
    assignee_id VARCHAR(36),

    -- 文档关联 (关联文档中心的文档 ID 列表)
    document_ids JSON,

    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (project_id) REFERENCES projects(id),
    FOREIGN KEY (parent_id) REFERENCES requirements(id),
    FOREIGN KEY (epic_id) REFERENCES requirements(id)
);

-- 任务表
CREATE TABLE tasks (
    id VARCHAR(36) PRIMARY KEY,
    requirement_id VARCHAR(36),
    project_id VARCHAR(36) NOT NULL,
    title VARCHAR(200) NOT NULL,
    description TEXT,
    type VARCHAR(30) NOT NULL,  -- requirement/design/ui/frontend/backend/test
    status VARCHAR(20) DEFAULT 'todo',  -- todo/in_progress/blocked/review/completed
    assignee_id VARCHAR(36),
    required_roles JSON,
    start_date DATETIME,
    end_date DATETIME,
    estimated_hours INTEGER,
    actual_hours INTEGER,
    parent_task_id VARCHAR(36),
    dependency_ids JSON,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    assigned_at DATETIME,
    accepted_at DATETIME,
    completed_at DATETIME,
    result JSON,
    FOREIGN KEY (requirement_id) REFERENCES requirements(id),
    FOREIGN KEY (project_id) REFERENCES projects(id),
    FOREIGN KEY (assignee_id) REFERENCES agents(id)
);

-- 文档表
CREATE TABLE documents (
    id VARCHAR(36) PRIMARY KEY,
    project_id VARCHAR(36) NOT NULL,
    requirement_id VARCHAR(36),
    task_id VARCHAR(36),
    name VARCHAR(200) NOT NULL,
    original_name VARCHAR(200),
    file_path VARCHAR(500) NOT NULL,
    file_type VARCHAR(50),
    file_size INTEGER,
    uploaded_by VARCHAR(36),
    version INTEGER DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (project_id) REFERENCES projects(id),
    FOREIGN KEY (requirement_id) REFERENCES requirements(id),
    FOREIGN KEY (task_id) REFERENCES tasks(id)
);

-- 审计日志表
CREATE TABLE audit_logs (
    id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36),
    action VARCHAR(50) NOT NULL,
    resource_type VARCHAR(30),
    resource_id VARCHAR(36),
    details JSON,
    ip_address VARCHAR(45),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 通知表
CREATE TABLE notifications (
    id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    title VARCHAR(200) NOT NULL,
    content TEXT,
    type VARCHAR(30) DEFAULT 'info',  -- info/success/warning/error
    is_read BOOLEAN DEFAULT FALSE,
    related_resource_type VARCHAR(30),
    related_resource_id VARCHAR(36),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    read_at DATETIME
);
```

---

## 七、前端页面设计

### 7.1 页面路由

```
/                       → 首页/仪表盘
/login                  → 管理员登录
/projects               → 项目列表
/projects/:id           → 项目详情
/projects/:id/reqs      → 需求管理
/projects/:id/tasks     → 任务看板
/projects/:id/docs      → 文档中心
/agents                 → Agent 管理
/agents/:id             → Agent 详情
/settings               → 系统设置
```

### 7.2 关键页面原型

**需求管理页（树形大纲视图）：**
```
┌─────────────────────────────────────────────────────────────────────────┐
│ 需求管理                              [+ 新增需求]  [树形/列表] [刷新]  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│ 📦 大需求：项目管理模块                                       [编辑] [...] │
│    类型：新功能  优先级：P0  状态：开发中                                │
│    关联文档：需求设计.md 概要设计.pdf                                    │
│    ┌──────────────────────────────────────────────────────────────────┐ │
│    │ 📦 子需求：项目列表查询                               [编辑] [...]  │ │
│    │    类型：新功能  优先级：P1  状态：开发中                         │ │
│    │    ┌────────────────────────────────────────────────────────────┐│ │
│    │    │ 📋 任务：列表查询前端开发              [编辑] [删除] [关联任务]││ │
│    │    │    类型：新功能  优先级：P1  状态：开发中                   ││ │
│    │    │    执行人：前端开发 Agent                                   ││ │
│    │    └────────────────────────────────────────────────────────────┘│ │
│    │    ┌────────────────────────────────────────────────────────────┐│ │
│    │    │ 📋 任务：列表查询后端开发              [编辑] [删除] [关联任务]││ │
│    │    │    类型：新功能  优先级：P1  状态：已完成                   ││ │
│    │    │    执行人：后端开发 Agent                                   ││ │
│    │    └────────────────────────────────────────────────────────────┘│ │
│    └──────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│ 📦 大需求：用户中心模块                                       [编辑] [...] │
│    类型：新功能  优先级：P0  状态：设计中                                │
│    关联文档：需求设计.md                                                 │
└─────────────────────────────────────────────────────────────────────────┘
```

**Agent 列表页：**
```
┌─────────────────────────────────────────────────────────────────┐
│ Agent 管理                           [+ 新增 Agent]  [刷新]     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🟢 PC2-项目经理    [project-manager]              10:00 在线  │ │
│ │ ─────────────────────────────────────────────────────────── │ │
│ │ 📌 当前任务：TASK-20260321-001                              │ │
│ │    标题：电商平台需求分析                                    │ │
│ │    项目：电商平台重构           [查看任务详情 →]             │ │
│ │    进度：████████░░░░ 80%                                   │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                  │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🟡 PC2-前端开发    [frontend-developer]            10:00 在线  │ │
│ │ ─────────────────────────────────────────────────────────── │ │
│ │ 空闲中 - 可以分配任务                                        │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                  │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🔴 PC2-后端开发    [backend-developer]             离线      │ │
│ │ ─────────────────────────────────────────────────────────── │ │
│ │ 最后在线：2 小时前                                           │ │
│ └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

**新增 Agent 弹窗：**
```
┌─────────────────────────────────────────────────┐
│ 新增 Agent                            [×]       │
├─────────────────────────────────────────────────┤
│ Agent 名称：[PC2-产品经理__________]            │
│                                                  │
│ 角色选择（可多选）：                             │
│ ☐ 项目经理                                      │
│ ☑ 产品经理                                      │
│ ☐ UI 设计师                                      │
│ ☐ 前端研发                                      │
│ ☐ 后端研发                                      │
│ ☐ 测试工程师                                    │
│                                                  │
│ 最大并发任务数：[3]                             │
│                                                  │
│ 能力标签：[task-decomposition] [progress-...]   │
│                                                  │
│            [取消]      [生成 SK 并创建]           │
└─────────────────────────────────────────────────┘
```

**任务分配弹窗：**
```
┌─────────────────────────────────────────────────┐
│ 分配任务                              [×]       │
├─────────────────────────────────────────────────┤
│ 任务：登录页面开发                               │
│ 项目：电商平台重构                               │
│ 需求：用户认证模块                               │
│                                                  │
│ 选择执行 Agent:                                 │
│ ┌─────────────────────────────────────────────┐│
│ │ 🟢 前端 Agent-01    空闲      [选择]        ││
│ │ 🟢 前端 Agent-02    执行中    [选择]        ││
│ │ 🟡 后端 Agent-01    空闲      [选择]        ││
│ │ 🔴 测试 Agent-01    离线      [禁用]        ││
│ └─────────────────────────────────────────────┘│
│                                                  │
│ 优先级：[P1 高 ▼]                               │
│ 截止时间：[2026-03-22 18:00]                    │
│                                                  │
│            [取消]      [确认分配]                │
└─────────────────────────────────────────────────┘
```

---

## 八、OpenCLAW 本地配置

### 8.1 配置文件结构

```
~/.openclaw/
├── role-config.json          # 角色配置（用户预先配置）
├── platform-config.json      # 平台配置（用户配置 URL+AK）
├── worker-sk.json            # Worker SK（注册后自动保存）
└── skills/
    └── team-collaboration/
        └── SKILL.md          # 团队协同 SKILL
```

### 8.2 配置文件模板

**role-config.json：**
```json
{
  "agentId": "pc2-worker",
  "name": "PC2-项目经理",
  "roles": ["project-manager"],
  "capabilities": ["task-decomposition", "progress-tracking", "result-summary"],
  "maxConcurrentTasks": 3
}
```

**platform-config.json：**
```json
{
  "url": "http://<server>:8000",
  "ak": "sk-xxxxxxxx"
}
```

**worker-sk.json：**
```json
{
  "worker_sk": "worker_sk_xxx",
  "agent_id": "uuid-xxx",
  "registered_at": "2026-03-21T10:00:00Z"
}
```

### 8.3 OpenCLAW 启动流程

```
1. 用户配置平台信息
   openclaw config.patch '{
     "platform": {"url": "http://x.x.x.x:8000", "ak": "sk-xxx"}
   }'

2. 用户加载 SKILL
   (自动加载 skills/team-collaboration/SKILL.md)

3. SKILL 初始化
   - 读取 ~/.openclaw/role-config.json
   - 读取 ~/.openclaw/platform-config.json
   - 检查 ~/.openclaw/worker-sk.json 是否存在

4. 如果 Worker SK 不存在
   - 调用 POST /api/agents/register 注册
   - 保存返回的 worker_sk 到 ~/.openclaw/worker-sk.json

5. 建立 WebSocket 连接
   - WS /ws/agent/connect?sk=worker_sk_xxx
   - 开始心跳（30 秒间隔）

6. 监听任务消息
   - 平台推送 → 处理任务
   - 角色匹配检查
   - 创建子代理执行
   - 上报进度到平台
```

---

## 九、开发计划与完成状态

### 阶段一：基础框架（已完成 ✅）

| 任务 | 说明 | 状态 |
|------|------|------|
| 项目脚手架搭建 | FastAPI 项目结构、Vue 3 项目结构 | ✅ 已完成 |
| 数据库模型实现 | SQLAlchemy 模型（含 3 级需求结构） | ✅ 已完成 |
| 基础 API 开发 | 用户登录、Agent 管理、项目管理 | ✅ 已完成 |
| WebSocket 框架 | 连接管理、消息路由 | ✅ 已完成 |

### 阶段二：核心功能（已完成 ✅）

| 任务 | 说明 | 状态 |
|------|------|------|
| Agent 注册/认证 | AK 换 SK 流程、心跳机制 | ✅ 已完成 |
| 任务管理 API | 创建、分配、领取、状态上报、看板视图 | ✅ 已完成 |
| WebSocket 消息处理 | 任务推送、心跳、进度上报 | ✅ 已完成 |
| 项目管理 API | 项目 CRUD、项目-Agent 关联管理 | ✅ 已完成 |
| **需求管理 API** | **3 级需求结构、树形查询、级联删除** | ✅ 已完成 |
| **文档管理 API** | **上传、下载、删除、关联** | ✅ 已完成 |
| **审计日志 API** | **操作记录、查询** | ✅ 已完成 |
| **通知中心 API** | **通知管理、已读未读状态** | ✅ 已完成 |

### 阶段三：前端开发（已完成 ✅）

| 任务 | 说明 | 状态 |
|------|------|------|
| 前端脚手架 | Vue 3 + Vite + Element Plus + Pinia | ✅ 已完成 |
| 登录页 | 管理员登录、JWT 认证 | ✅ 已完成 |
| 仪表盘页 | 项目统计、Agent 状态、任务概览 | ✅ 已完成 |
| Agent 管理页 | 列表、详情、新增、删除 | ✅ 已完成 |
| Agent 详情页 | Agent 信息、任务历史、心跳记录 | ✅ 已完成 |
| 项目列表页 | 项目列表、创建项目、项目状态 | ✅ 已完成 |
| 项目详情页 | 项目信息、成员管理、统计图表 | ✅ 已完成 |
| **需求管理页** | **树形大纲视图、增删改查、子需求创建、任务关联** | ✅ 已完成 |
| 任务看板页 | Kanban 视图、任务分配、状态流转 | ✅ 已完成 |
| 文档中心页 | 文档列表、上传、下载、预览 | ✅ 已完成 |
| 系统设置页 | 系统配置、用户管理 | ✅ 已完成 |

### 阶段四：UI 优化（已完成 ✅）

| 任务 | 说明 | 状态 |
|------|------|------|
| 现代化 UI 风格 | 渐变紫色主题 (#667eea → #764ba2) | ✅ 已完成 |
| 导航栏优化 | 子路径激活状态保持 | ✅ 已完成 |
| 响应式设计 | 侧边栏折叠、自适应布局 | ✅ 已完成 |

### 阶段五：联调测试（进行中 🔄）

| 任务 | 说明 | 状态 |
|------|------|------|
| 前后端联调 | API 对接、WebSocket 测试 | 🔄 进行中 |
| OpenCLAW SKILL 对接 | 与 OpenCLAW 联调 | ⏳ 待开始 |
| 集成测试 | 完整流程验证 | ⏳ 待开始 |
| 数据种子脚本 | 测试数据生成 | ✅ 已完成 |

---

## 十、已完成功能清单

### 10.1 后端 API（完整版）

| 模块 | 接口 | 状态 |
|------|------|------|
| 认证 | POST /api/auth/login | ✅ |
| 认证 | POST /api/auth/logout | ✅ |
| Agent | POST /api/agents/register | ✅ |
| Agent | GET /api/agents | ✅ |
| Agent | GET /api/agents/:id | ✅ |
| Agent | POST /api/agents/:id/heartbeat | ✅ |
| Agent | GET /api/agents/:id/tasks/pending | ✅ |
| Agent | POST /api/agents/tasks/:task_id/claim | ✅ |
| 项目 | GET /api/projects | ✅ |
| 项目 | POST /api/projects | ✅ |
| 项目 | GET /api/projects/:id | ✅ |
| 项目 | PUT /api/projects/:id | ✅ |
| 项目 | DELETE /api/projects/:id | ✅ |
| 项目 Agent | GET /api/projects/:id/agents | ✅ |
| 项目 Agent | POST /api/projects/:id/agents | ✅ |
| 项目 Agent | DELETE /api/projects/:id/agents/:agent_id | ✅ |
| 需求 | GET /api/requirements | ✅ |
| 需求 | POST /api/requirements | ✅ |
| 需求 | GET /api/requirements/:id | ✅ |
| 需求 | PUT /api/requirements/:id | ✅ |
| 需求 | DELETE /api/requirements/:id | ✅ |
| 需求 | POST /api/requirements/:id/status | ✅ |
| 需求 | GET /api/requirements/:id/children | ✅ |
| 需求 | GET /api/requirements/:id/tasks | ✅ |
| 任务 | GET /api/tasks | ✅ |
| 任务 | POST /api/tasks | ✅ |
| 任务 | GET /api/tasks/:id | ✅ |
| 任务 | POST /api/tasks/:id/assign | ✅ |
| 任务 | POST /api/tasks/:id/status | ✅ |
| 任务 | POST /api/tasks/:id/complete | ✅ |
| 任务 | GET /api/tasks/kanban/view | ✅ |
| 文档 | GET /api/documents | ✅ |
| 文档 | POST /api/documents/upload | ✅ |
| 文档 | GET /api/documents/:id | ✅ |
| 文档 | DELETE /api/documents/:id | ✅ |
| 文档 | GET /api/documents/:id/download | ✅ |
| 审计日志 | GET /api/audit-logs | ✅ |
| 任务分配记录 | GET /api/task-assignments/task/:task_id | ✅ |
| 任务分配记录 | GET /api/task-assignments/agent/:agent_id | ✅ |
| 通知中心 | GET /api/notifications | ✅ |
| 通知中心 | GET /api/notifications/unread-count | ✅ |
| 通知中心 | POST /api/notifications/:id/read | ✅ |
| 通知中心 | POST /api/notifications/read-all | ✅ |
| WebSocket | WS /ws/agent/connect | ✅ |

### 10.2 Services 层（完整版）

| 服务 | 功能 | 状态 |
|------|------|------|
| AuthService | 认证、Token 生成、管理员初始化 | ✅ |
| AgentService | Agent 注册、列表、心跳 | ✅ |
| ProjectService | 项目 CRUD、统计 | ✅ |
| TaskService | 任务 CRUD、分配、看板 | ✅ |
| DocumentService | 文档上传、下载、版本管理 | ✅ |
| AuditLogService | 审计日志记录、查询 | ✅ |
| ProjectAgentService | 项目-Agent 关联管理 | ✅ |
| TaskAssignmentService | 任务分配记录管理 | ✅ |
| NotificationService | 通知管理、消息推送 | ✅ |

### 10.3 前端页面（完整版）

| 页面 | 路由 | 状态 |
|------|------|------|
| 登录页 | /login | ✅ |
| 仪表盘 | / | ✅ |
| Agent 管理 | /agents | ✅ |
| Agent 详情 | /agents/:id | ✅ |
| 项目列表 | /projects | ✅ |
| 项目详情 | /projects/:id | ✅ |
| 需求管理 | /projects/:id/reqs | ✅ |
| 任务看板 | /projects/:id/tasks | ✅ |
| 文档中心 | /projects/:id/docs | ✅ |
| 系统设置 | /settings | ✅ |

---

## 十一、待完成功能

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 文档在线预览 | Markdown/PDF 在线预览 | 低 |
| 甘特图视图 | 任务时间线、依赖关系展示 | 低 |
| WebSocket 实时推送 | 通知实时推送、任务状态实时更新 | 中 |
| OpenCLAW SKILL 集成 | Agent 协同工作流程 | 高 |

---

## 十二、需要用户协助的事项

### 12.1 已完成事项

| 事项 | 说明 | 状态 |
|------|------|------|
| Admin 初始密码确认 | 使用 123456 | ✅ 已完成 |
| 端口配置 | 后端 30009，前端 3000 | ✅ 已完成 |
| 数据种子脚本 | 生成完整测试数据 | ✅ 已完成 |

### 12.2 OpenCLAW SKILL 开发配合

| 事项 | 说明 | 优先级 |
|------|------|--------|
| SKILL 适配 WebSocket | OpenCLAW 需要支持 WebSocket 连接 | 高 |
| 角色配置模板 | 提供各角色的 SKILL 工作制度模板 | 高 |
| 子代理调用规范 | 明确子代理创建和状态轮询接口 | 中 |

### 12.3 测试配合

| 事项 | 说明 | 优先级 |
|------|------|--------|
| OpenCLAW 实例 | 提供 1-2 个 OpenCLAW 实例用于联调 | 高 |
| 网络环境 | 确保局域网可访问公网平台 | 高 |

---

## 十三、下一步行动

1. **平台功能完善**
   - ✅ 已完成核心功能开发
   - 🔄 进行前后端联调测试
   - ⏳ 待完成 WebSocket 实时推送

2. **OpenCLAW 开发 SKILL**
   - 基于本设计方案调整 SKILL
   - 适配 WebSocket 连接
   - 实现角色约束逻辑

3. **联调测试**
   - 平台与 SKILL 对接
   - 完整流程验证

---

*文档结束*
